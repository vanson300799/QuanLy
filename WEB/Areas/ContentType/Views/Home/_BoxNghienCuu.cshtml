@model IEnumerable<WebModels.WebContent>
@{
    var webmodule = (WebModule)ViewBag.WebModule;
    var urlcat = webmodule.URL;
    if (string.IsNullOrEmpty(webmodule.URL) || string.IsNullOrWhiteSpace(webmodule.URL))
    {
        urlcat = Url.Action("Index", "Home", new { metatitle = webmodule.MetaTitle != null ? webmodule.MetaTitle : "web", id = webmodule.ID, area = "" });
    }
}


<div class="col-md-12">
    <div class="title-box">
        <a href="@urlcat" title="@webmodule.Title">  @webmodule.Title </a>
    </div>
</div>



<div class="col-md-6">
    @foreach (var item in Model)
    {
        var img = item.Image != null ? item.Image : "/images/no-image.jpg";

        var url = Url.Action("Detail", "Home", new
        {
            metatitle = item.MetaTitle != null ? item.MetaTitle : "tapchimoitruong",
            id = item.ID,
            m_metatitle = webmodule.MetaTitle != null ? webmodule.MetaTitle : "tapchimoitruong",
            m_id = webmodule.ID,
            area = ""
        });
        <div class="margin-top-20 big-box-news">
            @*<div class="margin-top-20 big-box-news">
                    <img class="card-img-top" src="@img" alt="@item.Title" />
                    <div class="card-body">
                        <a href="@url" title="@item.Title">
                            <h6 class="card-title">@Html.RawSubString(item.Title, 90)</h6>
                        </a>
                    </div>

                    <p class="card-text" style="font-style: italic; font-size: 12px;">
                        @if (!string.IsNullOrWhiteSpace(item.Description))
                        {
                            @Html.RawSubString(item.Description, 200)
                        }
                    </p>
                </div>*@
            <div class="single-blog-post d-flex">
                <div class="post-content" style="padding-left: 0px;">
                    <a href="@url" title="@item.Title" class="post-title">
                        <i class="fa fa-angle-double-right" aria-hidden="true"></i>
                        @Html.RawSubString(item.Title, 105)
                    </a>
                    <p class="single-blog-post-desc">
                        @if (!string.IsNullOrWhiteSpace(item.Description))
                        {
                            @Html.RawSubString(item.Description, 190)
                        }
                    </p>
                </div>
            </div>
        </div>
    }
</div>


<div class="col-md-6">
    <div class="margin-top-20" style="background: #ffff99; padding: 25px 15px;">
        <div class="single-blog-post d-flex" style="border-bottom: none; margin-bottom: 25px;">
            <div class="pull-left">
                <img src="~/Images/nghiencuu_icon1.png" style="width: 35px; margin-right: 35px;">
            </div>
            <div class="post-content" style="font-size: 18px;">
                <a href="/hoi-dong-bien-tap-30">
                    HỘI ĐỒNG BIÊN TẬP
                </a>
            </div>
        </div>
        <div class="single-blog-post d-flex" style="border-bottom: none; margin-bottom: 25px;">
            <div class="pull-left">
                <img src="~/Images/nghiencuu_icon2.png" style="width: 35px; margin-right: 35px;">
            </div>
            <div class="post-content" style="font-size: 18px;">
                <a href="/the-le-bai-viet-68">
                    THỂ LỆ BÀI VIẾT
                </a>
            </div>
        </div>
        <div class="single-blog-post d-flex" style="border-bottom: none;">
            <div class="pull-left">
                <img src="~/Images/nghiencuu_icon3.png" style="width: 35px; margin-right: 35px;">
            </div>
            <div class="post-content" style="font-size: 18px;">
                <a href="/thu-moi-viet-bai-tap-chi-so-dinh-ky-64">
                    GỬI BÀI TRỰC TUYẾN
                </a>
            </div>
        </div>
    </div>
</div>