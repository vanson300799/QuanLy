@*@model WebModels.WebModule

@if (Model != null)
{
    try
    {
        if (Model.PublishIndexLayout != null)
        {
            Layout = Model.PublishIndexLayout;
        }
        else
        {
            Layout = "~/Views/Shared/_Layout_02.cshtml";
        }
        @Html.Action(Model.PublishIndexView == null ? "_PubIndex" : Model.PublishIndexView, Model.ContentType.Controller, new { area = "ContentType", id = Model.ID })
    }
    catch (Exception) { }
}*@


